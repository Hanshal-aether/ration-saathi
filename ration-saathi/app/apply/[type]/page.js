"use client";import Link from"next/link";import{notFound}from"next/navigation";import{getType}from"../../../lib/config";import{useLang}from"../../../components/providers";export default function Check({params}){const t=getType(params.type);const{lang}=useLang();if(!t)return notFound();return <main className="mx-auto max-w-2xl px-4 py-8"><Link href="/" className="font-bold text-trust-700">← {lang==="hi"?"वापस":"Back"}</Link><p className="mt-7 font-semibold text-service-600">{lang==="hi"?"शुरू करने से पहले":"Before you begin"}</p><h1 className="mt-2 text-3xl font-bold">{lang==="hi"?t.hi:t.en}</h1><p className="mt-3 text-lg text-slate-600">{lang==="hi"?"ये दस्तावेज़ तैयार रखें।":"Keep these documents ready."}</p><div className="mt-6 grid gap-4">{t.docs.map((d,i)=><article key={d} className="rounded-2xl border bg-white p-5"><h2 className="text-lg font-bold">{["📄","🪪","✅"][i%3]} {d}</h2><p className="mt-1 text-base text-slate-600">A clear copy or photo.</p><details className="mt-3 rounded bg-service-50 p-3"><summary className="cursor-pointer font-bold text-service-700">{lang==="hi"?"मुझे इसकी ज़रूरत क्यों है?":"Why do I need this?"}</summary><p className="mt-2">This helps the local office check your request correctly.</p></details></article>)}</div><Link href={`/apply/${t.slug}/form`} className="mt-6 flex min-h-12 items-center justify-center rounded-xl bg-trust-700 text-lg font-bold text-white">{lang==="hi"?"आगे बढ़ें":"Continue"}</Link></main>}
