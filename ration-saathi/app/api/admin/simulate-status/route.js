import{NextResponse}from"next/server";import{prisma}from"../../../../lib/prisma";export async function POST(r){let{applicationId,status="needs_correction"}=await r.json();let a=await prisma.application.update({where:{id:applicationId},data:{status,statusReason:status==="needs_correction"?"Please re-upload a clear Aadhaar copy. The name spelling is not readable.":"Your request is moving to the next review stage."}});return NextResponse.json(a)}
